{
  "version": 2,
  "builds": [{ "src": "src/server.js", "use": "@now/node" }],
  "routes": [
    { "src": "/static/(.*)", "dest": "/public/$1" },
    { "src": "/(.*)", "dest": "/src/server.js" }
  ]
}